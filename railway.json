{
    "build": {
        "command": "composer install --no-dev && php artisan optimize:clear && php artisan optimize"
    },
    "deploy": {
        "command": "php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=8000"
    },
    "require": {
        "php": "^8.1",
        "ext-pdo": "*",
        "ext-pgsql": "*",
        "ext-mbstring": "*"
    }

}
